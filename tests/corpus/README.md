# Test corpus

Small, deliberately buggy C/Rust programs used to evaluate whyfault. Each directory has the source, a build script, and `expected.txt` describing the true root cause. Binaries are built into `bin/` (gitignored).
